{
  "name": "jivani-gems",
  "version": "1.0.0",
  "private": true,
  "description": "Jivani Gems — lab-grown diamond jewelry e-commerce (static front-end + Netlify serverless functions).",
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build step needed' && exit 0"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.0",
    "razorpay": "^2.9.4",
    "resend": "^4.0.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
